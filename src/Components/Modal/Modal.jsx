import React, { useContext } from "react";

import Style from "./Modal.styles";
import { IconCtx } from "react-icons/lib";
import { Line } from "react-icons/ri";

import ProgressiveBar from "../ProgressiveBar/ProgressiveBar";
import { Global } from "../../Global/Global";

export const Modal = ({ dataStatus, dataForm, dataProgress }) => {
    const global = useContext(Global);
}